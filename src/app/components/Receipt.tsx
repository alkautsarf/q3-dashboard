"use client";
import React from "react";

// Receipt: presentational summary before send (dumb component).
// Shows token, recipients, totals, balances, and gas comparison.
export default function Receipt() {
  return (
    <div className="hidden" aria-hidden>
      {/* Placeholder for receipt summary */}
    </div>
  );
}

